# sets generator options.
generator:
  # sets generator features.
  features:
    enable:
      - 'paths/client'
      - 'client/request/validation'
      - 'debug/example_tests'
    disable_all: true
