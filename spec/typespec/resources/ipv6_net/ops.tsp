// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for IPv6Net

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("IPv6Net")
interface IPv6NetOp {
  @summary("IPv6Net 一覧取得")
  @get
  @route("/{zone}/api/cloud/1.1/ipv6net")
  list(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @body body: IPv6NetListRequestEnvelope,
  ): IPv6NetListResponseEnvelope | ApiError;

  @summary("IPv6Net 取得")
  @get
  @route("/{zone}/api/cloud/1.1/ipv6net/{id}")
  read(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): IPv6NetReadResponseEnvelope | ApiError;
}
