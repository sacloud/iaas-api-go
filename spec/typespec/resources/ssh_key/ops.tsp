// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for SSHKey

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("SSHKey")
interface SSHKeyOp {
  @summary("SSHKey 一覧取得")
  @get
  @route("/sshkey")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): SSHKeyFindResponseEnvelope | ApiError;

  @summary("SSHKey 作成")
  @post
  @route("/sshkey")
  create(@body body: SSHKeyCreateRequestEnvelope): {
    @statusCode _: 201;
    ...SSHKeyCreateResponseEnvelope;
  } | ApiError;

  @summary("SSHKey 取得")
  @get
  @route("/sshkey/{id}")
  read(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): SSHKeyReadResponseEnvelope | ApiError;

  @summary("SSHKey 更新")
  @put
  @route("/sshkey/{id}")
  update(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: SSHKeyUpdateRequestEnvelope,
  ): SSHKeyUpdateResponseEnvelope | ApiError;

  @summary("SSHKey 削除")
  @delete
  @route("/sshkey/{id}")
  delete(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;
}
