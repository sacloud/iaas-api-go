// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Server

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Server")
interface ServerOp {
  @summary("Server 一覧取得")
  @get
  @route("/server")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): ServerFindResponseEnvelope | ApiError;

  @summary("Server 作成")
  @post
  @route("/server")
  create(@body body: ServerCreateRequestEnvelope): {
    @statusCode _: 201;
    ...ServerCreateResponseEnvelope;
  } | ApiError;

  @summary("Server 取得")
  @get
  @route("/server/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): ServerReadResponseEnvelope | ApiError;

  @summary("Server 更新")
  @put
  @route("/server/{id}")
  update(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerUpdateRequestEnvelope,
  ): ServerUpdateResponseEnvelope | ApiError;

  @summary("Server 削除")
  @delete
  @route("/server/{id}")
  delete(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerDeleteRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server プラン変更")
  @put
  @route("/server/{id}/plan")
  changePlan(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerChangePlanRequestEnvelope,
  ): ServerChangePlanResponseEnvelope | ApiError;

  @summary("Server CD-ROM 挿入")
  @put
  @route("/server/{id}/cdrom")
  insertCDROM(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerInsertCDROMRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server CD-ROM 取り出し")
  @delete
  @route("/server/{id}/cdrom")
  ejectCDROM(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerEjectCDROMRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server 起動")
  @put
  @route("/server/{id}/power")
  boot(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerBootRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server シャットダウン")
  @delete
  @route("/server/{id}/power")
  shutdown(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerShutdownRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server リセット")
  @put
  @route("/server/{id}/reset")
  reset(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server キー送信")
  @put
  @route("/server/{id}/keyboard")
  sendKey(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerSendKeyRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server NMI 送信")
  @put
  @route("/server/{id}/qemu/nmi")
  sendNMI(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Server VNC プロキシ取得")
  @get
  @route("/server/{id}/vnc/proxy")
  getVNCProxy(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): ServerGetVNCProxyResponseEnvelope | ApiError;

  @summary("Server モニター情報取得")
  @get
  @route("/server/{id}/monitor")
  monitor(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ServerMonitorRequestEnvelope,
  ): ServerMonitorResponseEnvelope | ApiError;
}
