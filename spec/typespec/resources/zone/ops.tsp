// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Zone

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Zone")
interface ZoneOp {
  @summary("ゾーン一覧取得")
  @get
  @route("/zone")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): ZoneFindResponseEnvelope | ApiError;

  @summary("ゾーン情報取得")
  @get
  @route("/zone/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): ZoneReadResponseEnvelope | ApiError;
}
