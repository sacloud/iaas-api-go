// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for PrivateHostPlan

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("PrivateHostPlan")
interface PrivateHostPlanOp {
  @summary("PrivateHostPlan 一覧取得")
  @get
  @route("/product/privatehost")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Class` (string): Appliance のサブクラス（例 `database` / `loadbalancer`）の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): PrivateHostPlanFindResponseEnvelope | ApiError;

  @summary("PrivateHostPlan 取得")
  @get
  @route("/product/privatehost/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): PrivateHostPlanReadResponseEnvelope | ApiError;
}
