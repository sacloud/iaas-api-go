// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Archive

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Archive")
interface ArchiveOp {
  @summary("Archive 一覧取得")
  @get
  @route("/{zone}/api/cloud/1.1/archive")
  find(
    @path zone: string,

    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
          - `Scope` (string): `"shared"` または `"user"` の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): ArchiveFindResponseEnvelope | ApiError;

  @summary("Archive 作成")
  @post
  @route("/{zone}/api/cloud/1.1/archive")
  create(@path zone: string, @body body: ArchiveCreateRequestEnvelope): {
    @statusCode _: 201;
    ...ArchiveCreateResponseEnvelope;
  } | ApiError;

  @summary("Archive 取得")
  @get
  @route("/{zone}/api/cloud/1.1/archive/{id}")
  read(
    @path zone: string,
    @path id: string,
  ): ArchiveReadResponseEnvelope | ApiError;

  @summary("Archive 更新")
  @put
  @route("/{zone}/api/cloud/1.1/archive/{id}")
  update(
    @path zone: string,
    @path id: string,
    @body body: ArchiveUpdateRequestEnvelope,
  ): ArchiveUpdateResponseEnvelope | ApiError;

  @summary("Archive 削除")
  @delete
  @route("/{zone}/api/cloud/1.1/archive/{id}")
  delete(@path zone: string, @path id: string): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Archive 共有")
  @put
  @route("/{zone}/api/cloud/1.1/archive/{id}/ftp")
  share(
    @path zone: string,
    @path id: string,
    @body body: ArchiveShareRequestEnvelope,
  ): ArchiveShareResponseEnvelope | ApiError;

  @summary("Archive FTP クローズ")
  @delete
  @route("/{zone}/api/cloud/1.1/archive/{id}/ftp")
  closeFTP(@path zone: string, @path id: string): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Archive 移管")
  @post
  @route("/{zone}/api/cloud/1.1/archive/{sourceArchiveID}/to/zone/{destZoneID}")
  transfer(
    @path zone: string,
    @path sourceArchiveID: string,
    @path destZoneID: string,
    @body body: ArchiveTransferRequestEnvelope,
  ): {
    @statusCode _: 201;
    ...ArchiveTransferResponseEnvelope;
  } | ApiError;
}
