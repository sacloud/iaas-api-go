// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Archive

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Archive")
interface ArchiveOp {
  @summary("Archive 一覧取得")
  @get
  @route("/archive")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
          - `Scope` (string): `"shared"` または `"user"` の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): ArchiveFindResponseEnvelope | ApiError;

  @summary("Archive 作成")
  @post
  @route("/archive")
  create(@body body: ArchiveCreateRequestEnvelope): {
    @statusCode _: 201;
    ...ArchiveCreateResponseEnvelope;
  } | ApiError;

  @summary("Archive 取得")
  @get
  @route("/archive/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): ArchiveReadResponseEnvelope | ApiError;

  @summary("Archive 更新")
  @put
  @route("/archive/{id}")
  update(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ArchiveUpdateRequestEnvelope,
  ): ArchiveUpdateResponseEnvelope | ApiError;

  @summary("Archive 削除")
  @delete
  @route("/archive/{id}")
  delete(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Archive 共有")
  @put
  @route("/archive/{id}/ftp")
  share(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: ArchiveShareRequestEnvelope,
  ): ArchiveShareResponseEnvelope | ApiError;

  @summary("Archive FTP クローズ")
  @delete
  @route("/archive/{id}/ftp")
  closeFTP(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Archive 移管")
  @post
  @route("/archive/{sourceArchiveID}/to/zone/{destZoneID}")
  transfer(
    @doc("""
      コピー元アーカイブ ID。
      """)
    @path
    sourceArchiveID: int64,

    @doc("""
      転送先ゾーンの ID。
      """)
    @path
    destZoneID: int64,

    @body body: ArchiveTransferRequestEnvelope,
  ): {
    @statusCode _: 201;
    ...ArchiveTransferResponseEnvelope;
  } | ApiError;
}
