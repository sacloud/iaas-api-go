// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Disk

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Disk")
interface DiskOp {
  @summary("Disk 一覧取得")
  @get
  @route("/disk")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
          - `Scope` (string): `"shared"` または `"user"` の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): DiskFindResponseEnvelope | ApiError;

  @summary("Disk 作成")
  @post
  @route("/disk")
  create(@body body: DiskCreateRequestEnvelope): {
    @statusCode _: 201;
    ...DiskCreateResponseEnvelope;
  } | ApiError;

  @summary("ディスクの修正反映")
  @put
  @route("/disk/{id}/config")
  config(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: DiskConfigRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Disk パーティションリサイズ")
  @put
  @route("/disk/{id}/resize-partition")
  resizePartition(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: DiskResizePartitionRequestEnvelope,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Disk サーバー接続")
  @put
  @route("/disk/{id}/to/server/{serverID}")
  connectToServer(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @doc("""
      サーバ ID。
      """)
    @path
    serverID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Disk サーバー切断")
  @delete
  @route("/disk/{id}/to/server")
  disconnectFromServer(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Disk 取得")
  @get
  @route("/disk/{id}")
  read(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): DiskReadResponseEnvelope | ApiError;

  @summary("Disk 更新")
  @put
  @route("/disk/{id}")
  update(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: DiskUpdateRequestEnvelope,
  ): DiskUpdateResponseEnvelope | ApiError;

  @summary("Disk 削除")
  @delete
  @route("/disk/{id}")
  delete(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Disk モニター情報取得")
  @get
  @route("/disk/{id}/monitor")
  monitor(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: DiskMonitorRequestEnvelope,
  ): DiskMonitorResponseEnvelope | ApiError;
}
