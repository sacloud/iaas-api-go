// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Interface

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Interface")
interface InterfaceOp {
  @summary("Interface 一覧取得")
  @get
  @route("/interface")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): InterfaceFindResponseEnvelope | ApiError;

  @summary("Interface 作成")
  @post
  @route("/interface")
  create(@body body: InterfaceCreateRequestEnvelope): {
    @statusCode _: 201;
    ...InterfaceCreateResponseEnvelope;
  } | ApiError;

  @summary("Interface 取得")
  @get
  @route("/interface/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): InterfaceReadResponseEnvelope | ApiError;

  @summary("Interface 更新")
  @put
  @route("/interface/{id}")
  update(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: InterfaceUpdateRequestEnvelope,
  ): InterfaceUpdateResponseEnvelope | ApiError;

  @summary("Interface 削除")
  @delete
  @route("/interface/{id}")
  delete(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Interface モニター情報取得")
  @get
  @route("/interface/{id}/monitor")
  monitor(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @body body: InterfaceMonitorRequestEnvelope,
  ): InterfaceMonitorResponseEnvelope | ApiError;

  @summary("Interface 共有セグメント接続")
  @put
  @route("/interface/{id}/to/switch/shared")
  connectToSharedSegment(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Interface スイッチ接続")
  @put
  @route("/interface/{id}/to/switch/{switchID}")
  connectToSwitch(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @doc("""
      スイッチ ID。
      """)
    @path
    switchID: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Interface スイッチ切断")
  @delete
  @route("/interface/{id}/to/switch")
  disconnectFromSwitch(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Interface パケットフィルタ接続")
  @put
  @route("/interface/{id}/to/packetfilter/{packetFilterID}")
  connectToPacketFilter(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,

    @doc("""
      パケットフィルタ ID。
      """)
    @path
    packetFilterID: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Interface パケットフィルタ切断")
  @delete
  @route("/interface/{id}/to/packetfilter")
  disconnectFromPacketFilter(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;
}
