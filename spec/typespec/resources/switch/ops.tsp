// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Switch

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Switch")
interface SwitchOp {
  @summary("Switch 一覧取得")
  @get
  @route("/switch")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
          - `Scope` (string): `"shared"` または `"user"` の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): SwitchFindResponseEnvelope | ApiError;

  @summary("Switch 作成")
  @post
  @route("/switch")
  create(@body body: SwitchCreateRequestEnvelope): {
    @statusCode _: 201;
    ...SwitchCreateResponseEnvelope;
  } | ApiError;

  @summary("Switch 取得")
  @get
  @route("/switch/{id}")
  read(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): SwitchReadResponseEnvelope | ApiError;

  @summary("Switch 更新")
  @put
  @route("/switch/{id}")
  update(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: SwitchUpdateRequestEnvelope,
  ): SwitchUpdateResponseEnvelope | ApiError;

  @summary("Switch 削除")
  @delete
  @route("/switch/{id}")
  delete(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("ブリッジ接続")
  @put
  @route("/switch/{id}/to/bridge/{bridgeID}")
  connectToBridge(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @doc("""
      ブリッジ ID。
      """)
    @path
    bridgeID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("ブリッジ切断")
  @delete
  @route("/switch/{id}/to/bridge")
  disconnectFromBridge(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Switch サーバー一覧取得")
  @get
  @route("/switch/{id}/server")
  getServers(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): SwitchGetServersResponseEnvelope | ApiError;
}
