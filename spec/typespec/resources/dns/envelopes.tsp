// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

import "@typespec/http";

namespace Sacloud.IaaS;

/**
 * Request envelope for dNSFindRequestEnvelope
 */
model DNSFindRequestEnvelope {
  @doc("取得する件数の上限 (ページングサイズ)")
  Count: int32;

  @doc("取得開始位置 (0 始まりのオフセット)")
  From: int32;

  @doc("検索条件を表す JSON オブジェクト。キーは \"{フィールド名}\" の形式で、値に完全一致や部分一致の条件を指定する")
  Filter: Record<unknown>;
}
/**
 * Response envelope for dNSFindResponseEnvelope
 */
@example(#{ CommonServiceItems: #[], Count: 0, From: 0, Total: 0 })
model DNSFindResponseEnvelope {
  @doc("対象リソースの総件数")
  Total: int32;

  @doc("現在のページ番号")
  From: int32;

  @doc("現在のページの件数")
  Count: int32;

  /**
   * CommonServiceItems
   */
  CommonServiceItems: DNS[];
}

/**
 * Request envelope for dNSCreateRequestEnvelope
 */
model DNSCreateRequestEnvelope {
  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem?: DNSCreateRequest;
}
/**
 * Response envelope for dNSCreateResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Description: "DNS ゾーン例",
    ID: 123456789012,
    Name: "example.com",
    Records: #[],
    Tags: #["example"],
  },
  is_ok: true,
})
model DNSCreateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: DNS;
}

/**
 * Response envelope for dNSReadResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Description: "DNS ゾーン例",
    ID: 123456789012,
    Name: "example.com",
    Records: #[#{ Name: "www", RData: "192.0.2.10", TTL: 3600, Type: "A" }],
    Tags: #["example"],
  },
  is_ok: true,
})
model DNSReadResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: DNS;
}

/**
 * Request envelope for dNSUpdateRequestEnvelope
 */
model DNSUpdateRequestEnvelope {
  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem?: DNSUpdateRequest;
}
/**
 * Response envelope for dNSUpdateResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Description: "更新後",
    ID: 123456789012,
    Name: "example.com",
    Records: #[],
    Tags: #["example", "updated"],
  },
  is_ok: true,
})
model DNSUpdateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: DNS;
}
