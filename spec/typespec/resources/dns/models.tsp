// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

import "@typespec/http";

namespace Sacloud.IaaS;

model DNS {
  ID: int64;
  Name: string;
  Description: string;
  Tags: string[];
  Availability: EAvailability;
  IconID: int64;

  // Go tags: `mapconv:"Icon.ID"`

  CreatedAt: utcDateTime;

  ModifiedAt: utcDateTime;
  Records: DNSRecord[];

  // Go tags: `mapconv:"Settings.DNS.[]ResourceRecordSets,recursive"`

  MonitoringSuiteLog: MonitoringSuiteLog;

  // Go tags: `mapconv:"Settings.DNS.MonitoringSuiteLog,recursive"`

  SettingsHash: string;

  // Go tags: `json:",omitempty" mapconv:",omitempty"`

  DNSZone: string;

  // Go tags: `mapconv:"Status.Zone"`

  DNSNameServers: string[];
  // Go tags: `mapconv:"Status.NS"`
}

model DNSRecord {
  Name: string;
  Type: EDNSRecordType;
  RData: string;
  TTL: int32;
}

model MonitoringSuiteLog {
  Enabled: boolean;
}

model DNSCreateRequest {
  Name: string;

  // Go tags: `mapconv:"Name/Status.Zone"`

  Records: DNSRecord[];

  // Go tags: `mapconv:"Settings.DNS.[]ResourceRecordSets,recursive"`

  MonitoringSuiteLog: MonitoringSuiteLog;

  // Go tags: `mapconv:"Settings.DNS.MonitoringSuiteLog,recursive"`

  Description: string;

  Tags: string[];
  IconID: int64;
  // Go tags: `mapconv:"Icon.ID"`
}

model DNSUpdateRequest {
  Description: string;
  Tags: string[];
  IconID: int64;

  // Go tags: `mapconv:"Icon.ID"`

  Records: DNSRecord[];

  // Go tags: `mapconv:"Settings.DNS.[]ResourceRecordSets,recursive"`

  MonitoringSuiteLog: MonitoringSuiteLog;

  // Go tags: `mapconv:"Settings.DNS.MonitoringSuiteLog,recursive"`

  SettingsHash: string;
  // Go tags: `json:",omitempty" mapconv:",omitempty"`
}

model DNSUpdateSettingsRequest {
  Records: DNSRecord[];

  // Go tags: `mapconv:"Settings.DNS.[]ResourceRecordSets,recursive"`

  MonitoringSuiteLog: MonitoringSuiteLog;

  // Go tags: `mapconv:"Settings.DNS.MonitoringSuiteLog,recursive"`

  SettingsHash: string;
  // Go tags: `json:",omitempty" mapconv:",omitempty"`
}
