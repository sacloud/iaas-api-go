// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Internet

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Internet")
interface InternetOp {
  @summary("Internet 一覧取得")
  @get
  @route("/{zone}/api/cloud/1.1/internet")
  find(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): InternetFindResponseEnvelope | ApiError;

  @summary("Internet 作成")
  @post
  @route("/{zone}/api/cloud/1.1/internet")
  create(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @body body: InternetCreateRequestEnvelope,
  ): {
    @statusCode _: 202;
    ...InternetCreateResponseEnvelope;
  } | ApiError;

  @summary("Internet 取得")
  @get
  @route("/{zone}/api/cloud/1.1/internet/{id}")
  read(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): InternetReadResponseEnvelope | ApiError;

  @summary("Internet 更新")
  @put
  @route("/{zone}/api/cloud/1.1/internet/{id}")
  update(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: InternetUpdateRequestEnvelope,
  ): InternetUpdateResponseEnvelope | ApiError;

  @summary("Internet 削除")
  @delete
  @route("/{zone}/api/cloud/1.1/internet/{id}")
  delete(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Internet 帯域更新")
  @put
  @route("/{zone}/api/cloud/1.1/internet/{id}/bandwidth")
  updateBandWidth(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: InternetUpdateBandWidthRequestEnvelope,
  ): InternetUpdateBandWidthResponseEnvelope | ApiError;

  @summary("Internet サブネット追加")
  @post
  @route("/{zone}/api/cloud/1.1/internet/{id}/subnet")
  addSubnet(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: InternetAddSubnetRequestEnvelope,
  ): {
    @statusCode _: 201;
    ...InternetAddSubnetResponseEnvelope;
  } | ApiError;

  @summary("Internet サブネット更新")
  @put
  @route("/{zone}/api/cloud/1.1/internet/{id}/subnet/{subnetID}")
  updateSubnet(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @doc("""
      サブネット ID。
      """)
    @path
    subnetID: string,

    @body body: InternetUpdateSubnetRequestEnvelope,
  ): InternetUpdateSubnetResponseEnvelope | ApiError;

  @summary("Internet サブネット削除")
  @delete
  @route("/{zone}/api/cloud/1.1/internet/{id}/subnet/{subnetID}")
  deleteSubnet(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @doc("""
      サブネット ID。
      """)
    @path
    subnetID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Internet モニター情報取得")
  @get
  @route("/{zone}/api/cloud/1.1/internet/{id}/monitor")
  monitor(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: InternetMonitorRequestEnvelope,
  ): InternetMonitorResponseEnvelope | ApiError;

  @summary("Internet IPv6 有効化")
  @post
  @route("/{zone}/api/cloud/1.1/internet/{id}/ipv6net")
  enableIPv6(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    ...InternetEnableIPv6ResponseEnvelope;
  } | ApiError;

  @summary("Internet IPv6 無効化")
  @delete
  @route("/{zone}/api/cloud/1.1/internet/{id}/ipv6net/{ipv6netID}")
  disableIPv6(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @path ipv6netID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;
}
