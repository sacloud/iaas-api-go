// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operations for Appliance shared endpoint group

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

model ApplianceCreateRequest {
  Class: string;
  Remark: ApplianceCreateRequestRemark;
  Settings?: unknown;
  Disk?: DatabaseDisk;
  Name: string;
  Description: string;
  Tags: string[];
  Icon?: ApplianceCreateRequestIcon;
  Plan?: ApplianceCreateRequestPlan;
}

model ApplianceCreateRequestRemark {
  Plan?: ApplianceCreateRequestRemarkPlan;
  Switch?: unknown;
  Servers?: ApplianceCreateRequestRemarkServers[];
  Network?: ApplianceCreateRequestRemarkNetwork;
  DBConf?: ApplianceCreateRequestRemarkDBConf;
  SourceAppliance?: ApplianceCreateRequestRemarkSourceAppliance;
  VRRP?: ApplianceCreateRequestRemarkVRRP;
  Router?: ApplianceCreateRequestRemarkRouter;
}

model ApplianceCreateRequestRemarkPlan {
  ID: int64;
}

model ApplianceCreateRequestRemarkServers {
  IPAddress: string;
}

model ApplianceCreateRequestRemarkNetwork {
  NetworkMaskLen?: int32;
  DefaultRoute?: string;
}

model ApplianceCreateRequestRemarkDBConf {
  Common: DatabaseRemarkDBConfCommon;
}

model ApplianceCreateRequestRemarkSourceAppliance {
  ID: int64;
}

model ApplianceCreateRequestRemarkVRRP {
  VRID: int32;
}

model ApplianceCreateRequestRemarkRouter {
  VPCRouterVersion: int32;
}

model ApplianceCreateRequestIcon {
  ID: int64;
}

model ApplianceCreateRequestPlan {
  ID: int64;
}

model ApplianceCreateRequestEnvelope {
  Appliance: ApplianceCreateRequest;
}

model ApplianceUpdateRequest {
  Name: string;
  Description: string;
  Tags: string[];
  Icon?: ApplianceUpdateRequestIcon;
  Settings?: unknown;
  SettingsHash?: string;
}

model ApplianceUpdateRequestIcon {
  ID: int64;
}

model ApplianceUpdateRequestEnvelope {
  Appliance: ApplianceUpdateRequest;
}

@tag("Appliance")
interface ApplianceOp {
  @summary("Appliance 一覧取得")
  @get
  @route("/appliance")
  find(
    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
          - `Class` (string): Appliance のサブクラス（例 `database` / `loadbalancer`）の部分一致
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): DatabaseFindResponseEnvelope | ApiError;

  @summary("Appliance 作成")
  @post
  @route("/appliance")
  create(@body body: ApplianceCreateRequestEnvelope): {
    @statusCode _: 202;
    ...DatabaseCreateResponseEnvelope;
  } | ApiError;

  @summary("Appliance 取得")
  @get
  @route("/appliance/{id}")
  read(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): DatabaseReadResponseEnvelope | ApiError;

  @summary("Appliance 更新")
  @put
  @route("/appliance/{id}")
  update(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: ApplianceUpdateRequestEnvelope,
  ): DatabaseUpdateResponseEnvelope | ApiError;

  @summary("Appliance 削除")
  @delete
  @route("/appliance/{id}")
  delete(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Appliance 設定反映")
  @put
  @route("/appliance/{id}/config")
  config(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Appliance 起動")
  @put
  @route("/appliance/{id}/power")
  boot(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Appliance シャットダウン")
  @delete
  @route("/appliance/{id}/power")
  shutdown(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: ShutdownOption,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Appliance リセット")
  @put
  @route("/appliance/{id}/reset")
  reset(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("Appliance CPU モニター情報取得")
  @get
  @route("/appliance/{id}/cpu/monitor")
  monitorCPU(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: MonitorCondition,
  ): DatabaseMonitorCPUResponseEnvelope | ApiError;

  @summary("Appliance インターフェイスモニター情報取得")
  @get
  @route("/appliance/{id}/interface/monitor")
  monitorInterface(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: MonitorCondition,
  ): DatabaseMonitorInterfaceResponseEnvelope | ApiError;

  @summary("Appliance データベースモニター情報取得")
  @get
  @route("/appliance/{id}/database/monitor")
  monitorDatabase(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @body body: MonitorCondition,
  ): DatabaseMonitorDatabaseResponseEnvelope | ApiError;

  @summary("Appliance ステータス取得")
  @get
  @route("/appliance/{id}/status")
  status(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): DatabaseStatusResponseEnvelope | ApiError;

  @summary("Appliance インターフェイスモニター情報取得")
  @get
  @route("/appliance/{id}/interface/{index}/monitor")
  monitorInterfaceByIndex(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,

    @doc("""
      インターフェースのインデックス（0 始まり）。
      """)
    @path
    index: string,
  ): MobileGatewayMonitorInterfaceResponseEnvelope | ApiError;
}

@tag("Database")
interface DatabaseOp {
  @summary("Database ディスクモニター情報取得")
  @get
  @route("/appliance/{id}/disk/0/monitor")
  monitorDisk(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    condition: MonitorCondition,
  ): DatabaseMonitorDiskResponseEnvelope | ApiError;

  @summary("Database パラメータ取得")
  @get
  @route("/appliance/{id}/database/parameter")
  getParameter(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): DatabaseGetParameterResponseEnvelope | ApiError;

  @summary("Database パラメータ設定")
  @put
  @route("/appliance/{id}/database/parameter")
  setParameter(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    param: Record<unknown>,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;
}

@tag("MobileGateway")
interface MobileGatewayOp {
  @summary("MobileGateway スイッチ接続")
  @put
  @route("/appliance/{id}/interface/1/to/switch/{switchID}")
  connectToSwitch(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    @doc("""
      スイッチ ID。
      """)
    @path
    switchID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway スイッチ切断")
  @delete
  @route("/appliance/{id}/interface/1/to/switch")
  disconnectFromSwitch(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway DNS 取得")
  @get
  @route("/appliance/{id}/mobilegateway/dnsresolver")
  getDNS(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayGetDNSResponseEnvelope | ApiError;

  @summary("MobileGateway DNS 設定")
  @put
  @route("/appliance/{id}/mobilegateway/dnsresolver")
  setDNS(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    param: MobileGatewayDNSSetting,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway SIM ルート取得")
  @get
  @route("/appliance/{id}/mobilegateway/simroutes")
  getSIMRoutes(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayGetSIMRoutesResponseEnvelope | ApiError;

  @summary("MobileGateway SIM ルート設定")
  @put
  @route("/appliance/{id}/mobilegateway/simroutes")
  setSIMRoutes(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    param: MobileGatewaySIMRouteParam[],
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway SIM 一覧取得")
  @get
  @route("/appliance/{id}/mobilegateway/sims")
  listSIM(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayListSIMResponseEnvelope | ApiError;

  @summary("MobileGateway SIM 追加")
  @post
  @route("/appliance/{id}/mobilegateway/sims")
  addSIM(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    param: MobileGatewayAddSIMRequest,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway SIM 削除")
  @delete
  @route("/appliance/{id}/mobilegateway/sims/{simID}")
  deleteSIM(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    @doc("""
      SIM ID。
      """)
    @path
    simID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway ログ取得")
  @get
  @route("/appliance/{id}/mobilegateway/sessionlog")
  logs(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayLogsResponseEnvelope | ApiError;

  @summary("MobileGateway トラフィック設定取得")
  @get
  @route("/appliance/{id}/mobilegateway/traffic_monitoring")
  getTrafficConfig(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayGetTrafficConfigResponseEnvelope | ApiError;

  @summary("MobileGateway トラフィック設定更新")
  @put
  @route("/appliance/{id}/mobilegateway/traffic_monitoring")
  setTrafficConfig(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    param: MobileGatewayTrafficControl,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway トラフィック設定削除")
  @delete
  @route("/appliance/{id}/mobilegateway/traffic_monitoring")
  deleteTrafficConfig(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("MobileGateway トラフィック状況取得")
  @get
  @route("/appliance/{id}/mobilegateway/traffic_status")
  trafficStatus(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): MobileGatewayTrafficStatusResponseEnvelope | ApiError;
}

@tag("VPCRouter")
interface VPCRouterOp {
  @summary("VPCRouter スイッチ接続")
  @put
  @route("/appliance/{id}/interface/{nicIndex}/to/switch/{switchID}")
  connectToSwitch(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    @doc("""
      NIC のインデックス（0 始まり）。
      """)
    @path
    nicIndex: string,
    @doc("""
      スイッチ ID。
      """)
    @path
    switchID: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("VPCRouter スイッチ切断")
  @delete
  @route("/appliance/{id}/interface/{nicIndex}/to/switch")
  disconnectFromSwitch(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    @doc("""
      NIC のインデックス（0 始まり）。
      """)
    @path
    nicIndex: string,
  ): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;

  @summary("VPCRouter Ping")
  @post
  @route("/appliance/{id}/vpcrouter/ping/{destination}")
  ping(
    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
    @doc("""
      ping の宛先 IP アドレスまたはホスト名。
      """)
    @path
    destination: string,
  ): {
    @statusCode _: 201;
    ...VPCRouterPingResponseEnvelope;
  } | ApiError;
}
