// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for AuthStatus

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("AuthStatus")
interface AuthStatusOp {
  @summary("認証情報取得")
  @get
  @route("/{zone}/api/cloud/1.1/auth-status")
  read(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,
  ): AuthStatusReadResponseEnvelope | ApiError;
}
