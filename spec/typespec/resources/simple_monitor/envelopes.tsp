// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

import "@typespec/http";

namespace Sacloud.IaaS;

/**
 * Request envelope for simpleMonitorFindRequestEnvelope
 */
model SimpleMonitorFindRequestEnvelope {
  @doc("取得する件数の上限 (ページングサイズ)")
  Count: int32;

  @doc("取得開始位置 (0 始まりのオフセット)")
  From: int32;

  @doc("検索条件を表す JSON オブジェクト。キーは \"{フィールド名}\" の形式で、値に完全一致や部分一致の条件を指定する")
  Filter: Record<unknown>;
}
/**
 * Response envelope for simpleMonitorFindResponseEnvelope
 */
@example(#{ CommonServiceItems: #[], Count: 0, From: 0, Total: 0 })
model SimpleMonitorFindResponseEnvelope {
  @doc("対象リソースの総件数")
  Total: int32;

  @doc("現在のページ番号")
  From: int32;

  @doc("現在のページの件数")
  Count: int32;

  /**
   * CommonServiceItems
   */
  CommonServiceItems: SimpleMonitor[];
}

/**
 * Request envelope for simpleMonitorCreateRequestEnvelope
 */
model SimpleMonitorCreateRequestEnvelope {
  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem?: SimpleMonitorCreateRequest;
}
/**
 * Response envelope for simpleMonitorCreateResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Class: "simplemon",
    Description: "シンプル監視例",
    ID: 123456789012,
    Name: "example.com",
    Tags: #["example"],
  },
  is_ok: true,
})
model SimpleMonitorCreateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: SimpleMonitor;
}

/**
 * Response envelope for simpleMonitorReadResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Class: "simplemon",
    Description: "シンプル監視例",
    ID: 123456789012,
    Name: "example.com",
    Tags: #["example"],
  },
  is_ok: true,
})
model SimpleMonitorReadResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: SimpleMonitor;
}

/**
 * Request envelope for simpleMonitorUpdateRequestEnvelope
 */
model SimpleMonitorUpdateRequestEnvelope {
  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem?: SimpleMonitorUpdateRequest;
}
/**
 * Response envelope for simpleMonitorUpdateResponseEnvelope
 */
@example(#{
  CommonServiceItem: #{
    Class: "simplemon",
    Description: "更新後",
    ID: 123456789012,
    Name: "example.com",
    Tags: #["example", "updated"],
  },
  is_ok: true,
})
model SimpleMonitorUpdateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  @doc("CommonServiceItem (各種付加サービス) の設定ペイロード")
  CommonServiceItem: SimpleMonitor;
}

/**
 * Request envelope for simpleMonitorMonitorResponseTimeRequestEnvelope
 */
model SimpleMonitorMonitorResponseTimeRequestEnvelope {
  /**
   * Start
   */
  Start: utcDateTime;

  /**
   * End
   */
  End: utcDateTime;
}
/**
 * Response envelope for simpleMonitorMonitorResponseTimeResponseEnvelope
 */
@example(#{ Data: #{}, is_ok: true })
model SimpleMonitorMonitorResponseTimeResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * Data
   */
  Data: unknown;
}

/**
 * Response envelope for simpleMonitorHealthStatusResponseEnvelope
 */
@example(#{ SimpleMonitor: #{ Health: "up", LatestLogs: #[] }, is_ok: true })
model SimpleMonitorHealthStatusResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * SimpleMonitor
   */
  SimpleMonitor: SimpleMonitorHealthStatus;
}
