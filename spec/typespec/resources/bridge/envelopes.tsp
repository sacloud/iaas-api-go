// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

import "@typespec/http";

namespace Sacloud.IaaS;

/**
 * Request envelope for bridgeFindRequestEnvelope
 */
model BridgeFindRequestEnvelope {
  @doc("取得する件数の上限 (ページングサイズ)")
  Count: int32;

  @doc("取得開始位置 (0 始まりのオフセット)")
  From: int32;

  @doc("検索条件を表す JSON オブジェクト。キーは \"{フィールド名}\" の形式で、値に完全一致や部分一致の条件を指定する")
  Filter: Record<unknown>;
}
/**
 * Response envelope for bridgeFindResponseEnvelope
 */
@example(#{
  Bridges: #[
    #{
      Description: "ドキュメント用ダミーブリッジ",
      ID: 123456789012,
      Name: "example-bridge",
    }
  ],
  Count: 1,
  From: 0,
  Total: 1,
})
model BridgeFindResponseEnvelope {
  @doc("対象リソースの総件数")
  Total: int32;

  @doc("現在のページ番号")
  From: int32;

  @doc("現在のページの件数")
  Count: int32;

  /**
   * Bridges
   */
  Bridges: Bridge[];
}

/**
 * Request envelope for bridgeCreateRequestEnvelope
 */
model BridgeCreateRequestEnvelope {
  /**
   * Bridge
   */
  Bridge: BridgeCreateRequest;
}
/**
 * Response envelope for bridgeCreateResponseEnvelope
 */
@example(#{
  Bridge: #{
    Description: "ドキュメント用ダミーブリッジ",
    ID: 123456789012,
    Name: "example-bridge",
  },
  is_ok: true,
})
model BridgeCreateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * Bridge
   */
  Bridge: Bridge;
}

/**
 * Response envelope for bridgeReadResponseEnvelope
 */
@example(#{
  Bridge: #{
    Description: "ドキュメント用ダミーブリッジ",
    ID: 123456789012,
    Name: "example-bridge",
  },
  is_ok: true,
})
model BridgeReadResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * Bridge
   */
  Bridge: Bridge;
}

/**
 * Request envelope for bridgeUpdateRequestEnvelope
 */
model BridgeUpdateRequestEnvelope {
  /**
   * Bridge
   */
  Bridge: BridgeUpdateRequest;
}
/**
 * Response envelope for bridgeUpdateResponseEnvelope
 */
@example(#{
  Bridge: #{
    Description: "更新後の説明",
    ID: 123456789012,
    Name: "example-bridge-updated",
  },
  is_ok: true,
})
model BridgeUpdateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * Bridge
   */
  Bridge: Bridge;
}
