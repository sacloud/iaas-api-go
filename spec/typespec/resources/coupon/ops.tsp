// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Coupon

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Coupon")
interface CouponOp {
  @summary("クーポン情報取得")
  @get
  @route("/{zone}/api/system/1.0/coupon/{accountID}")
  find(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: string,
  ): CouponFindResponseEnvelope | ApiError;
}
