// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Bill

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Bill")
interface BillOp {
  @summary("契約別請求情報取得")
  @get
  @route("/bill/by-contract/{accountID}")
  byContract(
    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: int64,
  ): BillByContractResponseEnvelope | ApiError;

  @summary("契約・年別請求情報取得")
  @get
  @route("/bill/by-contract/{accountID}/{year}")
  byContractYear(
    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: int64,

    @doc("""
      対象年（西暦 4 桁の 10 進文字列）。
      """)
    @path
    year: string,
  ): BillByContractYearResponseEnvelope | ApiError;

  @summary("契約・年月別請求情報取得")
  @get
  @route("/bill/by-contract/{accountID}/{year}/{month}")
  byContractYearMonth(
    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: int64,

    @doc("""
      対象年（西暦 4 桁の 10 進文字列）。
      """)
    @path
    year: string,

    @doc("""
      対象月（1〜12 の 10 進文字列）。
      """)
    @path
    month: string,
  ): BillByContractYearMonthResponseEnvelope | ApiError;

  @summary("請求情報取得")
  @get
  @route("/bill/id/{id}")
  read(
    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): BillReadResponseEnvelope | ApiError;

  @summary("請求明細取得")
  @get
  @route("/billdetail/{MemberCode}/{id}")
  details(
    @doc("""
      会員コード。
      """)
    @path
    MemberCode: string,

    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): BillDetailsResponseEnvelope | ApiError;

  @summary("請求明細 CSV 取得")
  @get
  @route("/billdetail/{MemberCode}/{id}/csv")
  detailsCSV(
    @doc("""
      会員コード。
      """)
    @path
    MemberCode: string,

    @doc("""
      対象リソースの ID。
      """)
    @path
    id: int64,
  ): BillDetailsCSVResponseEnvelope | ApiError;
}
