// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for Bill

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("Bill")
interface BillOp {
  @summary("契約別請求情報取得")
  @get
  @route("/{zone}/api/system/1.0/bill/by-contract/{accountID}")
  byContract(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: string,
  ): BillByContractResponseEnvelope | ApiError;

  @summary("契約・年別請求情報取得")
  @get
  @route("/{zone}/api/system/1.0/bill/by-contract/{accountID}/{year}")
  byContractYear(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: string,

    @doc("""
      対象年（西暦 4 桁）。
      """)
    @path
    year: string,
  ): BillByContractYearResponseEnvelope | ApiError;

  @summary("契約・年月別請求情報取得")
  @get
  @route("/{zone}/api/system/1.0/bill/by-contract/{accountID}/{year}/{month}")
  byContractYearMonth(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      契約（アカウント）の ID。
      """)
    @path
    accountID: string,

    @doc("""
      対象年（西暦 4 桁）。
      """)
    @path
    year: string,

    @doc("""
      対象月（1〜12）。
      """)
    @path
    month: string,
  ): BillByContractYearMonthResponseEnvelope | ApiError;

  @summary("請求情報取得")
  @get
  @route("/{zone}/api/system/1.0/bill/id/{id}")
  read(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): BillReadResponseEnvelope | ApiError;

  @summary("請求明細取得")
  @get
  @route("/{zone}/api/system/1.0/billdetail/{MemberCode}/{id}")
  details(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      会員コード。
      """)
    @path
    MemberCode: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): BillDetailsResponseEnvelope | ApiError;

  @summary("請求明細 CSV 取得")
  @get
  @route("/{zone}/api/system/1.0/billdetail/{MemberCode}/{id}/csv")
  detailsCSV(
    @doc("""
      リソースが所属するゾーンの識別子。`tk1a` / `tk1b` / `is1a` / `is1b` / `is1c` のいずれか。Sandbox 環境では `tk1v` を指定する。
      """)
    @path
    zone: string,

    @doc("""
      会員コード。
      """)
    @path
    MemberCode: string,

    @doc("""
      対象リソースの ID。数値を 10 進文字列で指定する。
      """)
    @path
    id: string,
  ): BillDetailsCSVResponseEnvelope | ApiError;
}
