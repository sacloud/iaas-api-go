// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

import "@typespec/http";

namespace Sacloud.IaaS;

/**
 * Request envelope for privateHostFindRequestEnvelope
 */
model PrivateHostFindRequestEnvelope {
  @doc("取得する件数の上限 (ページングサイズ)")
  Count: int32;

  @doc("取得開始位置 (0 始まりのオフセット)")
  From: int32;

  @doc("検索条件を表す JSON オブジェクト。キーは \"{フィールド名}\" の形式で、値に完全一致や部分一致の条件を指定する")
  Filter: Record<unknown>;
}
/**
 * Response envelope for privateHostFindResponseEnvelope
 */
@example(#{ Count: 0, From: 0, PrivateHosts: #[], Total: 0 })
model PrivateHostFindResponseEnvelope {
  @doc("対象リソースの総件数")
  Total: int32;

  @doc("現在のページ番号")
  From: int32;

  @doc("現在のページの件数")
  Count: int32;

  /**
   * PrivateHosts
   */
  PrivateHosts: PrivateHost[];
}

/**
 * Request envelope for privateHostCreateRequestEnvelope
 */
model PrivateHostCreateRequestEnvelope {
  /**
   * PrivateHost
   */
  PrivateHost: PrivateHostCreateRequest;

  @doc("配置先の専有ストレージ契約ID (任意)")
  TargetDedicatedStorageContract?: unknown;
}
/**
 * Response envelope for privateHostCreateResponseEnvelope
 */
@example(#{
  PrivateHost: #{
    Description: "専有ホスト例",
    ID: 123456789012,
    Name: "example-private-host",
    Tags: #["example"],
  },
  is_ok: true,
})
model PrivateHostCreateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * PrivateHost
   */
  PrivateHost: PrivateHost;
}

/**
 * Response envelope for privateHostReadResponseEnvelope
 */
@example(#{
  PrivateHost: #{
    Description: "専有ホスト例",
    ID: 123456789012,
    Name: "example-private-host",
    Tags: #["example"],
  },
  is_ok: true,
})
model PrivateHostReadResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * PrivateHost
   */
  PrivateHost: PrivateHost;
}

/**
 * Request envelope for privateHostUpdateRequestEnvelope
 */
model PrivateHostUpdateRequestEnvelope {
  /**
   * PrivateHost
   */
  PrivateHost: PrivateHostUpdateRequest;
}
/**
 * Response envelope for privateHostUpdateResponseEnvelope
 */
@example(#{
  PrivateHost: #{
    Description: "更新後",
    ID: 123456789012,
    Name: "example-private-host-updated",
    Tags: #["example", "updated"],
  },
  is_ok: true,
})
model PrivateHostUpdateResponseEnvelope {
  @doc("オペレーションが成功したかどうかを示すフラグ。成功判定にはこのフィールドを用いること。")
  is_ok: boolean;

  /**
   * PrivateHost
   */
  PrivateHost: PrivateHost;
}
