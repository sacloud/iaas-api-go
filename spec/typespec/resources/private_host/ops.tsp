// generated by 'github.com/sacloud/iaas-api-go/internal/tools/gen-typespec'; DO NOT EDIT

// TypeSpec operation definition for PrivateHost

import "@typespec/http";

using TypeSpec.Http;

namespace Sacloud.IaaS;

@tag("PrivateHost")
interface PrivateHostOp {
  @summary("PrivateHost 一覧取得")
  @get
  @route("/{zone}/api/cloud/1.1/privatehost")
  find(
    @path zone: string,

    @doc("""
      Find 検索条件をシリアライズした JSON 文字列を渡す。
      
      **指定可能なトップレベルフィールド:**
      
      - `Count` (int): 取得件数の上限
      - `From` (int): 開始オフセット
      - `Filter` (object): このエンドポイントで指定可能なフィルタキーは以下
          - `Name` (string): 部分一致。スペース区切りで AND 結合
          - `Tags` (string[]): タグ完全一致の AND 結合
      """)
    @example("""
      {"Count":10,"From":0,"Filter":{"Name":"foo"}}
      """)
    @query
    q?: string,
  ): PrivateHostFindResponseEnvelope | ApiError;

  @summary("PrivateHost 作成")
  @post
  @route("/{zone}/api/cloud/1.1/privatehost")
  create(@path zone: string, @body body: PrivateHostCreateRequestEnvelope): {
    @statusCode _: 201;
    ...PrivateHostCreateResponseEnvelope;
  } | ApiError;

  @summary("PrivateHost 取得")
  @get
  @route("/{zone}/api/cloud/1.1/privatehost/{id}")
  read(
    @path zone: string,
    @path id: string,
  ): PrivateHostReadResponseEnvelope | ApiError;

  @summary("PrivateHost 更新")
  @put
  @route("/{zone}/api/cloud/1.1/privatehost/{id}")
  update(
    @path zone: string,
    @path id: string,
    @body body: PrivateHostUpdateRequestEnvelope,
  ): PrivateHostUpdateResponseEnvelope | ApiError;

  @summary("PrivateHost 削除")
  @delete
  @route("/{zone}/api/cloud/1.1/privatehost/{id}")
  delete(@path zone: string, @path id: string): {
    @statusCode _: 200;
    is_ok: boolean;
  } | ApiError;
}
