# v2(work in progress)

> [!IMPORTANT]
> 現在、v2 は開発中です。予告なくインターフェースが変更されることがあります。

iaas-api-go v2 です。v1 とは互換性はありません。

## v2 での変更点

v1 では go の dsl を定義し、そこから client を生成していました。
v2 では、openapi 定義からクライアントを自動生成するように変更されています。

